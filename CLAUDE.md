# CLAUDE.md

## 기본수칙
- 한국어로 대답해줘
- 반말로 대답해줘
- 나를 '형'이라고 호칭해줘

## PDCA 워크플로우
모든 작업은 사이클 단위로 진행해줘. 사이클 구분이 스펙에 정의되어 있으면 그걸 따르고, 없으면 Plan 단계에서 사이클 분할을 먼저 제안해줘. **한 번에 한 사이클만.** 사이클을 건너뛰거나 병합하지 말아줘.

### Plan (사이클 시작 시)
- 이번 사이클에 대해 아래 3가지를 선언한 뒤 시작해줘:
  1. **범위**: 이번 사이클에서 만들 것 / 만들지 않을 것
  2. **변경 파일**: 생성·수정할 파일 목록
  3. **검증 방법**: Check 단계에서 무엇을 어떻게 실행해서 확인할지 (가능하면 기대값을 구체적 숫자·결과로)
- 선언은 간결하게 10줄 이내로. 선언 후 바로 Do로 진행해도 돼 (Plan 승인은 불필요).

### Do (구현)
- Plan에서 선언한 범위만 구현해줘. 범위 밖 개선거리가 눈에 띄면 구현하지 말고 메모만 남겨줘.
- 다음 사이클에 필요한 코드를 미리 작성하지 말아줘 (YAGNI).
- 에러 처리는 스펙에 명시된 것 위주로, 과도한 방어 코드는 지양해줘.

### Check (검증) — 가장 중요
- **실행하지 않은 것은 검증된 것이 아니야.** "동작할 것으로 보입니다"는 금지. 반드시 실제로 명령·테스트를 실행하고 그 출력을 근거로 보고해줘.
- 스펙에 검증 기준(테스트 시나리오, 기대값)이 정의되어 있으면 그 기준 전체를 실행해줘. 기대값이 있는 항목은 기대값과 실제값을 나란히 비교하고, **하나라도 다르면 Check 실패야.**
- 검증 결과 보고 형식:
  ```
  ## Check 결과 (사이클 이름)
  - 실행한 것: (명령/방법)
  - 결과: ✅/❌ 항목별로
  - 기대값 vs 실제값: (숫자·결과 검증이 있는 경우)
  - 발견한 문제: (없으면 "없음")
  ```

### Act (조정)
- Check 실패 시: 원인 분석 → 수정 → **Check 전체 재실행** (수정한 부분만 재확인하지 말 것).
- 같은 문제로 3회 실패하면 시도를 멈추고, 시도한 접근들과 실패 원인을 정리해서 보고해줘. 방향은 형이 정할게.
- 수정 과정에서 스펙과 어긋나는 결정이 필요해지면 임의로 바꾸지 말고 먼저 물어봐줘.

### 승인 게이트
- Check 결과 보고 후 **형의 명시적 승인("다음 진행" 등)이 있기 전까지 다음 사이클에 착수하지 말아줘.**
- 승인 대기 중에는 다음 사이클 코드를 미리 작성하지 말아줘.

## typescript
- strict 모드 기준으로 작성해줘. `any` 사용은 불가피한 경우에만 하고 이유를 주석으로 남겨줘.
- 외부 입력 검증은 스키마 라이브러리(zod 등)로 해줘.

## python
- 항상 PEP8을 준수하여 작성해줘

## git
- 불필요한 파일은 git add 하지 말아줘 (.env*, node_modules, 빌드 산출물 절대 금지)
- 커밋은 **사이클당 1개**, Check 통과 후에만 커밋해줘
- 메시지는 본문은 작성하지 않고, 제목 한 줄만 작성해줘
- 메시지는 git diff를 보고 변경사항을 종합해줘
- 사이클 기반 작업일 때 제목 형식: `타입: {사이클} 요약` (예: `feat: C2 지출 입력 API 구현`)

**타입:**
- `feat`: 새로운 기능 추가
- `fix`: 버그 수정
- `optimize`: 성능 최적화
- `refine`: 기능 개선 및 리팩토링
- `docs`: 문서 수정
- `chore`: 빌드, 설정 파일 수정
